import 'package:flutter/material.dart';

class Publicas extends StatefulWidget {
  @override
  _PublicasState createState() => _PublicasState();
}

class _PublicasState extends State<Publicas> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Publicas'),),
      body: ListView(children: <Widget>[

      ],),
    );
  }
}
